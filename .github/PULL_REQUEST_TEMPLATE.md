## Summary

<!-- 1-3 sentences. Why does this change exist? -->

## Changes

<!-- Concrete list. Prefer verbs: adds, removes, fixes, refactors. -->

- 

## Test plan

- [ ] `make check` green locally
- [ ] Coverage did not drop below CI floor (see `.github/workflows/ci.yml`)
- [ ] Manually exercised the affected code path

## Roadmap / trade-offs

<!-- If this touches a ROADMAP item, name it. If it introduces a
     temporary trade-off (e.g. lower coverage on a specific package),
     say so and link the follow-up. -->

## Docs

- [ ] Updated `README.md` if the user-visible surface changed
- [ ] Updated `docs/ROADMAP.md` if a planned item is now shipped
- [ ] `revive` still clean on exported identifiers
